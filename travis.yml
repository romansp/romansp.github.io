sudo: false
language: node_js
node_js:
  - node

script:
  - npm install
  - npm run docs:build

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_ACCESS_TOKEN
  target-branch: gh-pages
  local-dir: public
  on:
    branch: master
